# Cloudflare Worker 配置
name = "gemini-image-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
API_BASE_URL = "https://api.zxvmax.com"

# API_KEY 需要通过 wrangler secret put API_KEY 设置
# 不要在这里明文写入
